---
title: All OTT repositories
---

# All OTT repositories

OTT maintains at least <%= Repos.active.size %> active repositories including apps, services, gems and documentation.

## Repos by type

| Type | Count | Repos |
| --- | --- | --- |
<% Repos.active.group_by(&:type).sort.each do |name, repos| %>
| # <%= name %> | <%= repos.count %> | <%= repos.map { |repo| "[#{repo.repo_name}](/repos/#{repo.repo_name}.html)" }.join(", ") %> |
<% end %>

## Repos by team

| Team | Count | Repos |
| --- | --- | --- |
<% Repos.active.group_by(&:team).sort.each do |name, repos| %>
| # <%= name %> | <%= repos.count %> | <%= repos.map { |repo| "[#{repo.repo_name}](/repos/#{repo.repo_name}.html)" }.join(", ") %> |
<% end %>
